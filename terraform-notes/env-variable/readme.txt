In this project we are setting the environment variable so that we no need to enter the value at the terminal.

to set the environment variable we will use the below in the terminal.

export TF_VAR_<variable_name>=value
